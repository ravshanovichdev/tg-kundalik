# 📊 Инструкция по импорту базы данных в phpMyAdmin

## 🚀 Быстрый способ

### Вариант 1: Импорт через phpMyAdmin (Рекомендуется)

1. **Откройте phpMyAdmin**
   - Обычно доступен по адресу: `http://localhost/phpmyadmin` или `http://localhost:8080`

2. **Создайте новую базу данных** (если еще не создана)
   - Нажмите на вкладку "Базы данных" (Databases)
   - Введите имя: `samit_global`
   - Выберите кодировку: `utf8mb4_unicode_ci`
   - Нажмите "Создать" (Create)

3. **Импортируйте SQL файл**
   - Выберите базу данных `samit_global` в левом меню
   - Перейдите на вкладку "Импорт" (Import)
   - Нажмите "Выберите файл" (Choose File)
   - Выберите файл `database_schema.sql`
   - Нажмите "Вперед" (Go)

4. **Проверьте результат**
   - Должны быть созданы 7 таблиц:
     - `users` - Пользователи
     - `teachers` - Учителя
     - `groups` - Группы/Классы
     - `students` - Ученики
     - `attendance` - Посещаемость
     - `grades` - Оценки
     - `payments` - Платежи

### Вариант 2: Через командную строку MySQL

```bash
# Если используете Docker
docker compose exec db mysql -u samit_user -p samit_global < database_schema.sql

# Или напрямую
mysql -u root -p samit_global < database_schema.sql
```

### Вариант 3: Копирование SQL в phpMyAdmin

1. Откройте файл `database_schema.sql` в текстовом редакторе
2. Скопируйте весь SQL код
3. В phpMyAdmin выберите базу данных `samit_global`
4. Перейдите на вкладку "SQL"
5. Вставьте скопированный код
6. Нажмите "Вперед" (Go)

## 📋 Структура базы данных

### Таблицы и их назначение:

1. **users** - Все пользователи системы (admin, teacher, parent)
   - `telegram_id` - уникальный ID из Telegram
   - `role` - роль пользователя
   - `is_active` - активен ли пользователь

2. **teachers** - Профили учителей
   - Связана с `users` через `user_id`
   - Содержит информацию об учителе

3. **groups** - Учебные группы/классы
   - Связана с `teachers` через `teacher_id`
   - Содержит расписание и стоимость

4. **students** - Ученики
   - Связана с `users` (родитель) через `parent_id`
   - Связана с `groups` через `group_id`

5. **attendance** - Посещаемость
   - Связана с `students` и `groups`
   - Статусы: PRESENT, ABSENT, LATE

6. **grades** - Оценки
   - Связана с `students` и `groups`
   - Типы: exam, homework, test, quiz

7. **payments** - Платежи
   - Связана с `students` и `groups`
   - Статусы: PAID, UNPAID, OVERDUE

## 🔍 Проверка после импорта

Выполните в phpMyAdmin SQL запрос:

```sql
-- Проверка всех таблиц
SHOW TABLES;

-- Проверка структуры таблицы users
DESCRIBE users;

-- Подсчет записей в каждой таблице
SELECT 
    'users' as table_name, COUNT(*) as count FROM users
UNION ALL
SELECT 'teachers', COUNT(*) FROM teachers
UNION ALL
SELECT 'groups', COUNT(*) FROM groups
UNION ALL
SELECT 'students', COUNT(*) FROM students
UNION ALL
SELECT 'attendance', COUNT(*) FROM attendance
UNION ALL
SELECT 'grades', COUNT(*) FROM grades
UNION ALL
SELECT 'payments', COUNT(*) FROM payments;
```

## ⚠️ Важные замечания

1. **Кодировка**: Все таблицы используют `utf8mb4_unicode_ci` для поддержки эмодзи и всех языков
2. **Внешние ключи**: Включены для целостности данных
3. **Индексы**: Созданы для оптимизации запросов
4. **Тестовые данные**: В файле есть примеры тестовых пользователей (можно удалить)

## 🔧 Настройка после импорта

После успешного импорта убедитесь, что:

1. **Переменные окружения** в `.env` указывают на правильную базу:
   ```env
   MYSQL_HOST=localhost
   MYSQL_USER=root
   MYSQL_PASSWORD=your_password
   MYSQL_DATABASE=samit_global
   MYSQL_PORT=3306
   ```

2. **Права доступа** настроены правильно для пользователя БД

3. **Backend** может подключиться к базе данных

## 📞 Troubleshooting

### Ошибка: "Table already exists"
- Удалите существующие таблицы или используйте `DROP DATABASE samit_global;` перед импортом

### Ошибка: "Access denied"
- Проверьте права пользователя MySQL
- Убедитесь, что пользователь имеет права CREATE, INSERT, SELECT

### Ошибка: "Unknown collation"
- Убедитесь, что MySQL версии 8.0 или выше
- Или измените `utf8mb4_unicode_ci` на `utf8_general_ci`

## ✅ Готово!

После успешного импорта база данных готова к использованию с вашим приложением.

