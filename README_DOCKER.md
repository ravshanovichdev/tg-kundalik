# 🐳 Docker Setup - Краткая инструкция

## Быстрый старт

```bash
# 1. Создайте .env файл (скопируйте из env-example.txt)
cp env-example.txt .env

# 2. Отредактируйте .env с вашими настройками
nano .env

# 3. Запустите все сервисы
docker compose up -d

# 4. Инициализируйте базу данных
make db-init
```

## Доступ к сервисам

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **Database**: localhost:3306

## Основные команды

```bash
make up          # Запустить все
make down        # Остановить все
make logs        # Просмотр логов
make db-init     # Инициализировать БД
make clean       # Очистить все
```

Подробная документация: [DOCKER.md](./DOCKER.md)

